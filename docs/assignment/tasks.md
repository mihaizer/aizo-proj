# Tasks List

## Implemented
- [x] Replace temporary `main.cpp` demo with a real CLI runner based on `Parameters`.
- [x] Reject unsupported algorithms, structures and data types with clear errors.
- [x] Require `--inputFile` in both `singleFile` and `benchmark` modes.
- [x] Add dynamic array copy support for benchmark iterations.
- [x] Add singly linked list and doubly linked list structures.
- [x] Add separate sorting classes for insertion sort, quicksort and bucket sort.
- [x] Add file loading and output writing for `size + values` text format.
- [x] Add ascending-order verification without using built-in sorting.
- [x] Measure only sorting time with `std::chrono`.
- [x] Append benchmark results to CSV with min, average and max time.

## Next
- [ ] Review bucket sort behavior with the professor's exact expectations.
- [ ] Add larger benchmark runs for all required algorithm/structure combinations.
- [ ] Prepare final report tables and charts from generated CSV files.

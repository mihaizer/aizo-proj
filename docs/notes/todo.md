- recompile lib from downloads
надо будет написать тесты
Сразу продумай скрипт (bash/python) или обертку в C++, которая сама переберет все размеры массивов (Badanie A, B, C) и сгенерит готовый .csv лог.
скрипт на генерацию csv ранзых размеров
логи как я понял пишим постфактум а не во время раоботы алгормтма чтоб не таомазить его

добавить меншон шо я взял код с репы препода
